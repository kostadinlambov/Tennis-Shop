package kl.tennisshop.models.bindingModels;

public class BindingModel {

    // Binding Model (DTO)
    // Model(DTO) от FrontEnd --> BackEnd

    // Трябва да направим валидацията на полетата тук!!!
}
